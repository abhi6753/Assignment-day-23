//7. Write a C++ program to calculate the square of a number
#include<iostream>
using namespace std;
int main()
{
    int a;
    cout<<"Enter number to calculate its square: ";
    cin>>a;
    cout<<"Square of "<<a<<"is"<<a*a;
    cin.get();
    return 0;
}